language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t canya145/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true canya145/docker-react npm run test